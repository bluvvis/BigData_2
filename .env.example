# Copy to ".env" (Compose loads it automatically). ".env" is gitignored.
# After PIPELINE COMPLETE, cluster-master stays up — no 15–20 min rerun for docker exec / screenshots.
STAY_ALIVE_AFTER_PIPELINE=1
